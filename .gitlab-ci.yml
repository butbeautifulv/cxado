# Corp cxado — Fabrica oss-full-enterprise + k3s Kubernetes runner (Kaniko/Helm)
#
# Runner: deploy/k8s/gitlab-runner/ (tags: k8s, corp, cxado)
# Bootstrap: ./scripts/gitlab/setup-runner-k8s.sh bootstrap
# CI vars: ./scripts/gitlab/setup-ci-variables.sh
# Sync corp GitLab: ./scripts/gitlab/sync-monorepo-to-gitlab.sh

include:
  - local: '.gitlab/ci/cxado-k3s.gitlab-ci.yml'
