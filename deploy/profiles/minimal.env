# cxado-minimal: agents on host, veil graph-lite + no k3s/kafka/qdrant.
# Usage: make cxado-up-minimal
#        WORKER_REPLICAS=1 make -C projects/egregore dev

CXADO_PROFILE=minimal
WORKER_REPLICAS=1
OTEL_ENABLED=false
USE_DYNAMIC_CATALOG=true
