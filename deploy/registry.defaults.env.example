# Copy to deploy/.secrets/cxado-k3s.env and fill secrets.
# Registry defaults: deploy/registry.defaults.env (override only if corp endpoints change)

NEXUS_DOCKER_REGISTRY=nexus.svo.aero:8345
NEXUS_DOCKER_GROUP_REGISTRY=nexus.svo.aero:8374
NEXUS_API_URL=https://nexus.svo.aero:8443
NEXUS_PYPI_HOST=nexus.svo.aero:8443
NEXUS_CXADO_DOCKER_REPO=cxado-docker
NEXUS_PYPI_REPO=srsips-pypi

NEXUS_USER=
NEXUS_PASSWORD=
