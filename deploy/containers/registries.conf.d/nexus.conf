# Nexus Docker proxy (docker.io → nexus.svo.aero:8345).
# Install: scripts/podman-nexus-setup.sh
# API/UI: https://nexus.svo.aero:8443/  |  Docker registry: nexus.svo.aero:8345

unqualified-search-registries = ["docker.io"]

[[registry]]
prefix = "docker.io"
location = "nexus.svo.aero:8345"
insecure = true
