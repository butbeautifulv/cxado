This folder is for **local-only** secrets used for offline deployments.

- Do not commit secrets.
- Files in this folder should match `.gitignore` patterns.

Recommended file:
- `deploy/.secrets/cxado-k3s.env` (export-style env vars)

