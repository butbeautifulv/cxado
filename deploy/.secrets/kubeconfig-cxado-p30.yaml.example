# Copy to kubeconfig-cxado-p30.yaml after running:
#   ./scripts/k8s/lens-export-kubeconfig.sh --with-tunnel
#
# Gitignored output path: deploy/.secrets/kubeconfig-cxado-p30.yaml
# Cluster name in Lens: cxado-p30
# API via SSH tunnel: https://127.0.0.1:16443
