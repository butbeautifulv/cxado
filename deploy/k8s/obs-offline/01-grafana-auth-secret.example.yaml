apiVersion: v1
kind: Secret
metadata:
  name: grafana-auth
  namespace: cxado-obs
type: Opaque
stringData:
  # Change before applying. Keep this file as an example only.
  admin_password: "CHANGE_ME_ADMIN_PASSWORD"
  viewer_username: "viewer"
  viewer_password: "CHANGE_ME_VIEWER_PASSWORD"
