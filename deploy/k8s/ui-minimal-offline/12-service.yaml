apiVersion: v1
kind: Service
metadata:
  name: egregore-ui-minimal
  namespace: cxado-edge
  labels:
    app: egregore-ui-minimal
spec:
  type: ClusterIP
  selector:
    app: egregore-ui-minimal
  ports:
    - name: http
      port: 80
      targetPort: http
