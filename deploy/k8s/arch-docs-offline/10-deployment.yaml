apiVersion: apps/v1
kind: Deployment
metadata:
  name: cxado-arch-docs
  namespace: cxado-edge
  labels:
    app: cxado-arch-docs
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cxado-arch-docs
  template:
    metadata:
      labels:
        app: cxado-arch-docs
    spec:
      enableServiceLinks: false
      containers:
        - name: nginx
          image: docker.io/nginx:1.27-alpine
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 80
          volumeMounts:
            - name: site
              mountPath: /usr/share/nginx/html
              readOnly: true
            - name: nginx-config
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf
              readOnly: true
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 15
      volumes:
        - name: site
          hostPath:
            path: /home/bbv/cxado/arch-docs
            type: Directory
        - name: nginx-config
          configMap:
            name: cxado-arch-docs-nginx
