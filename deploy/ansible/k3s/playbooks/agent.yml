---
- name: Prepare worker node (common prep)
  hosts: k3s_agent
  gather_facts: true
  roles:
    - cxado_common
    - cxado_ssh
    - k3s_common
    - k3s_hostpaths

- name: Join k3s worker nodes
  hosts: k3s_agent
  gather_facts: false
  roles:
    - k3s_agent
