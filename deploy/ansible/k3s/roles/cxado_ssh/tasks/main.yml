---
- name: Install SSH drop-in for cxado dual-port access
  ansible.builtin.template:
    src: sshd-cxado-wifi.conf.j2
    dest: "/etc/ssh/sshd_config.d/{{ ssh_dropin_name }}"
    owner: root
    group: root
    mode: "0644"
  notify: Reload ssh
