# Docker compose lite profile (cxado-up-minimal): Prometheus only — no Tempo/Loki.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
