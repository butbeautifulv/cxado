apiVersion: v1
kind: Service
metadata:
  name: {{ include "siem-mcp.fullname" . }}
  labels:
    {{- include "siem-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "siem-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
