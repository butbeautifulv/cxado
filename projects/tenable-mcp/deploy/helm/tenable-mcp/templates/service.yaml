apiVersion: v1
kind: Service
metadata:
  name: {{ include "tenable-mcp.fullname" . }}
  labels:
    {{- include "tenable-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "tenable-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
