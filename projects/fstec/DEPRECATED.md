# Deprecated — use Tabula submodule

FSTEC product code now lives at **`projects/tabula/fstec`** (submodule inside [tabula](https://github.com/butbeautifulv/tabula)).

| Old path | New path |
|----------|----------|
| `projects/fstec/fstec` | `projects/tabula/fstec` |
| `projects/fstec/cxado-gui` | `shared/gui` (`@cxado/gui` hub) |

This folder is kept temporarily for local reference. Do not add new work here.

After `make bootstrap`:

```bash
cd projects/tabula/fstec
```
