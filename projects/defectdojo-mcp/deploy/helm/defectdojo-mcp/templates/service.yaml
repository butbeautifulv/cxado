apiVersion: v1
kind: Service
metadata:
  name: {{ include "defectdojo-mcp.fullname" . }}
  labels:
    {{- include "defectdojo-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "defectdojo-mcp.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
