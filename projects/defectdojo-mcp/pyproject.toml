[project]
name = "defectdojo-mcp"
version = "0.1.0"
description = "MCP server for DefectDojo API v2 vulnerability management"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "fastmcp>=2.12.0",
    "httpx>=0.28.0",
    "pydantic-settings>=2.6.0",
    "tenacity>=9.0.0",
    "uvicorn[standard]>=0.32.0",
]

[project.scripts]
defectdojo-mcp = "defectdojo_mcp.main:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/defectdojo_mcp"]

[tool.hatch.build.targets.sdist]
include = ["src/defectdojo_mcp", "docs", "scripts"]

[dependency-groups]
dev = [
    "pytest>=8.3.0",
    "pytest-asyncio>=0.24.0",
    "respx>=0.22.0",
    "ruff>=0.8.0",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
