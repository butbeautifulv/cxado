# Awareness domain (archived — out of cxado scope)

**Status:** removed from cxado monorepo (2026-07).

| Was | Now |
|-----|-----|
| `projects/hexenhammer` submodule | `~/Desktop/hexenhammer` (private GitHub repo) |

Product code and docs live in the standalone clone. This file is kept only as a historical pointer for architecture-site links.
